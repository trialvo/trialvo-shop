-- Deprecated placeholder. Real trial admin credentials are injected by:
--   Option 1: trialvo-backend dockerProvisioner.seedTrialAdmin()
--   Option 2: scripts/seed-trial-admin.js (called from seed-trial.sh)
SELECT 'trial_admin_seed OK — use seed-trial-admin.js' AS status;
