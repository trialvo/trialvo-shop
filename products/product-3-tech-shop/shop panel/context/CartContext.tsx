/**
 * @deprecated Cart is powered by Redux Toolkit (`store/cart`).
 * These re-exports keep existing import paths working.
 */
export type { CartItem } from "@/store/cart/types";
export { useCart } from "@/hooks/useCart";
