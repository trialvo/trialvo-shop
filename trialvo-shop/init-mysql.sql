-- Deprecated: use ../../infra/init-databases.sql via infra/docker-compose.yml
-- Kept so older docs that reference this path still resolve.
CREATE DATABASE IF NOT EXISTS trialvo_shop
  CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
